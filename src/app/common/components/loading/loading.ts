export interface LoadingInterfaceProgress {
  name: string;
  context: string;
  title: string;
  text: string;
  progress: number;
  progressDescription: string;
}

export interface LoadingInterface {
  name: string;
  context: string;
  title: string;
  text: string;
}